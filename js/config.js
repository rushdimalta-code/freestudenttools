/**
 * FreeStudentTools — Site Configuration
 * Add your Google Maps API key below.
 * The same key used for Gemini API works if Maps Embed API is enabled in Google Cloud Console.
 * Restrict the key to your domain (freestudenttools.com) in Google Cloud Console → Credentials.
 */
window.FST_CONFIG = {
  GOOGLE_MAPS_KEY: '', // ← Paste your Google Maps / Gemini project API key here
};
